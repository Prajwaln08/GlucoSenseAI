# ══════════════════════════════════════════════════════════
# GlucoSense AI — Serving Requirements (API + Celery worker)
#
# Intentionally excludes:
#   torch, pytorch-forecasting, neuralforecast, neuralprophet  (training only)
#   optuna                                                      (HPO only)
#   mlflow                                                      (training only)
#   dvc, dvc-gdrive, gdown                                      (data pipeline only)
#   matplotlib, seaborn                                         (evaluation plots only)
# ══════════════════════════════════════════════════════════

# ── Environment ───────────────────────────────────────────
python-dotenv==1.0.1

# ── Core ML (inference only) ─────────────────────────────
lightgbm==4.5.0
xgboost==2.1.3
scikit-learn==1.5.2

# ── Data ──────────────────────────────────────────────────
pandas==2.2.3
numpy==1.26.4
pyarrow==16.1.0
scipy==1.13.1

# ── API ───────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1  # 4.1+ breaks passlib 1.7.4
python-multipart==0.0.9
jinja2==3.1.4  # server-rendered patient web UI (src/web)

# ── Database ──────────────────────────────────────────────
sqlalchemy==2.0.30
alembic==1.13.1
asyncpg==0.29.0
psycopg2-binary==2.9.9
sqlalchemy-cockroachdb==2.0.2

# ── Task queue ────────────────────────────────────────────
celery[redis]==5.4.0
redis==5.2.1

# ── HTTP client (Junction API) ────────────────────────────
httpx==0.27.0

# ── Monitoring ────────────────────────────────────────────
loguru==0.7.2
prometheus-fastapi-instrumentator==7.0.0
